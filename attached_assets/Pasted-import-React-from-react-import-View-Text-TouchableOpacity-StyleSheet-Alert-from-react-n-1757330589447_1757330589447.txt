import React from "react";
import { View, Text, TouchableOpacity, StyleSheet, Alert } from "react-native";

const NotificationShortcuts = () => {
  // Example handlers for buttons
  const handleEnableAll = () => {
    Alert.alert("Action", "All notifications enabled");
    // Call your API or state logic here
  };

  const handleDisableAll = () => {
    Alert.alert("Action", "All notifications disabled");
    // Call your API or state logic here
  };

  const handleDisableEmails = () => {
    Alert.alert("Action", "All email notifications disabled");
    // Call your API or state logic here
  };

  const handleReset = () => {
    Alert.alert("Action", "Notifications reset to default");
    // Call your API or state logic here
  };

  return (
    <View style={styles.container}>
      <Text style={styles.title}>Shortcuts</Text>
      <Text style={styles.description}>
        Quickly manage all your preferences at once.
      </Text>

      <View style={styles.buttonGroup}>
        <TouchableOpacity style={styles.button} onPress={handleEnableAll}>
          <Text style={styles.buttonText}>Enable all</Text>
        </TouchableOpacity>

        <TouchableOpacity style={styles.button} onPress={handleDisableAll}>
          <Text style={styles.buttonText}>Disable all</Text>
        </TouchableOpacity>

        <TouchableOpacity style={styles.button} onPress={handleDisableEmails}>
          <Text style={styles.buttonText}>Disable emails</Text>
        </TouchableOpacity>

        <TouchableOpacity style={styles.button} onPress={handleReset}>
          <Text style={styles.buttonText}>Reset to default</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: { padding: 16, backgroundColor: "#fff", marginBottom: 24 },
  title: { fontSize: 18, fontWeight: "bold", marginBottom: 8 },
  description: { fontSize: 14, color: "#555", marginBottom: 12 },
  buttonGroup: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-between",
  },
  button: {
    backgroundColor: "#007BFF",
    pad
